{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "jquery",
      "jquery.timepicker"
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
